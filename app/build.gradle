apply plugin: 'com.android.application'

android {
    signingConfigs {
        config {
            keyAlias 'private_key_vox_spatium'
            keyPassword 'ted^2MMei&*334'
            storeFile file('/home/happy/Dane/VoxSpatium.org/Git i GooglePlay/private_key_to_sign_app_in_AndroidStudio.jks')
            storePassword 'dyW$%#cdpSS'
        }
    }
    compileSdkVersion 23
    buildToolsVersion "23.0.3"

    defaultConfig {
        applicationId "org.voxspatium"
        minSdkVersion 14
        targetSdkVersion 28 //testowane było na 23
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    testCompile 'junit:junit:4.12'
    compile 'com.android.support:appcompat-v7:23.4.0'
    compile 'com.android.support:design:23.4.0'
    compile project(':commons-net-3.6')
}
